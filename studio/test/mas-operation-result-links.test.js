/**
 * Lightweight `mode='links'` was removed when the search-result render was
 * unified onto the full mode (inspect/link buttons + per-row hydration).
 * Tests intentionally absent — the relevant assertions now live in any
 * future `mas-operation-result.test.js` that covers the unified renderer.
 */
