import { css } from 'lit';
export const styles = css`
    :host {
        display: block;
        padding: 24px;
    }
    .card {
        background: #fff;
        padding: 16px;
        border-radius: 12px;
        margin-bottom: 16px;
    }
    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 16px;
    }
`;
