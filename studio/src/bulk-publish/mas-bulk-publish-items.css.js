import { css } from 'lit';
export const styles = css`
    :host {
        display: block;
    }
    .warning {
        color: var(--spectrum-negative-background-color-default, #da1e28);
        padding: 8px 0;
    }
`;
