# Checkout Link

## Overview

Enhanced anchor element that generates checkout URLs. Handles offer resolution and checkout parameter generation.

## Usage

```html
<a[is="checkout-link"]></a[is="checkout-link"]>
```
